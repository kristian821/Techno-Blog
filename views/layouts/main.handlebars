<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Techno-Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
</head>
<body>
    <header class="bg-dark bg-gradient text-light">
        <h1>
            <a href="/" class="text-light text-decoration-none">Techno-Blog</a>
        </h1>
        <nav>
            <a href="/dashboard" class="btn text-light text-decoration-none">Dashboard</a>
             {{#if loggedIn}}
            <button class="btn logout-btn text-light text-decoration-none">Logout</button>
            {{else}}
            <a href="/login" class="btn text-light text-decoration-none">Login</a>
            {{/if}}
        </nav>
    </header>
    <main>
        <div class="container">
            <div class="row">
                <div class="col"></div>
                <div class="col">
                    {{{ body }}}
                </div>
                <div class="col"></div>
            </div>
        </div>
    </main>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
    
</body>
</html>