 
        <span class="author card-subtitle text-muted p-3 col-auto">Posted by {{user.username}} on {{format_date created_at}}</span>
        <div class="body card-text">
            <div class="p-3">
                {{post_text}}
            </div>
        </div>